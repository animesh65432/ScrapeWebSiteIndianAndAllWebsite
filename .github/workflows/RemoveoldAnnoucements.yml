name: Clean Data JSON Files

on:
  workflow_dispatch:
  schedule:
    - cron: "30 18 * * *"

# Add permissions for the workflow
permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Use the default GITHUB_TOKEN with write permissions
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Display data directory before cleanup
        run: |
          echo "📁 Data directory structure before cleanup:"
          ls -la data/ || echo "No data directory found"
          find data/ -name "*.json" -type f | wc -l || echo "0"

      - name: Run cleanup script
        run: |
          python3 -m utils.remove_old_annoucements

      - name: Display data directory after cleanup
        run: |
          echo "📁 Data directory structure after cleanup:"
          ls -la data/ || echo "No data directory found"
          find data/ -name "*.json" -type f | wc -l || echo "0"

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Commit only if there are changes
          if [[ -n $(git status --porcelain) ]]; then
            git add data/
            git commit -m "🗑️ Automated cleanup: Remove JSON files from data directory"
            git push origin main
            echo "✅ Changes committed and pushed"
          else
            echo "✅ No changes to commit"
          fi

      - name: Summary
        run: |
          echo "## Cleanup Summary 🧹" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Cleanup workflow completed successfully!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📊 Remaining JSON files: $(find data/ -name "*.json" -type f 2>/dev/null | wc -l)" >> $GITHUB_STEP_SUMMARY
