name: Clean Data JSON Files

on:
  workflow_dispatch:
  schedule:
    - cron: "30 18 * * *" # 12:00 AM IST (midnight)

permissions:
  contents: write

jobs:
  start-ec2:
    runs-on: ubuntu-latest
    outputs:
      instance-id: ${{ steps.start.outputs.instance-id }}
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Start EC2
        id: start
        run: |
          STATE=$(aws ec2 describe-instances --instance-ids ${{ secrets.EC2_INSTANCE_ID }} \
            --query 'Reservations[0].Instances[0].State.Name' --output text)

          if [ "$STATE" = "stopped" ]; then
            aws ec2 start-instances --instance-ids ${{ secrets.EC2_INSTANCE_ID }}
            aws ec2 wait instance-running --instance-ids ${{ secrets.EC2_INSTANCE_ID }}
            echo "✅ EC2 started! Waiting 90s for runner to connect..."
            sleep 90
          elif [ "$STATE" = "running" ]; then
            echo "✅ EC2 already running! Waiting 30s..."
            sleep 30
          else
            echo "❌ Instance in unexpected state: $STATE"
            exit 1
          fi

          echo "instance-id=${{ secrets.EC2_INSTANCE_ID }}" >> $GITHUB_OUTPUT

  cleanup-github:
    needs: start-ec2
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Display data directory before cleanup
        run: |
          echo "📁 Data directory structure before cleanup:"
          ls -la data/ || echo "No data directory found"
          echo ""
          echo "📊 JSON file count:"
          find data/ -name "*.json" -type f 2>/dev/null | wc -l || echo "0"
          echo ""
          echo "📂 Data folder sizes:"
          du -sh data/*/ 2>/dev/null || echo "No data folders"

      - name: Run cleanup script
        run: |
          echo "🗑️ Running cleanup script..."
          python3 -m utils.remove_old_annoucements
          echo "✅ Cleanup script completed!"

      - name: Display data directory after cleanup
        run: |
          echo "📁 Data directory structure after cleanup:"
          ls -la data/ || echo "No data directory found"
          echo ""
          echo "📊 JSON file count:"
          find data/ -name "*.json" -type f 2>/dev/null | wc -l || echo "0"
          echo ""
          echo "📂 Data folder sizes:"
          du -sh data/*/ 2>/dev/null || echo "No data folders"

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Commit only if there are changes
          if [[ -n $(git status --porcelain) ]]; then
            git add data/
            git commit -m "🗑️ Automated cleanup: Remove old JSON files - $(date +'%Y-%m-%d %H:%M:%S')"
            
            # Pull with rebase to handle any remote changes, then push
            max_retries=3
            retry_count=0
            
            while [ $retry_count -lt $max_retries ]; do
              echo "Attempt $((retry_count + 1)) of $max_retries to push changes..."
              
              # Pull latest changes with rebase
              git pull --rebase origin main
              
              # Try to push
              if git push origin main; then
                echo "✅ Changes committed and pushed successfully"
                break
              else
                retry_count=$((retry_count + 1))
                if [ $retry_count -lt $max_retries ]; then
                  echo "⚠️ Push failed, retrying in 5 seconds..."
                  sleep 5
                else
                  echo "❌ Failed to push after $max_retries attempts"
                  exit 1
                fi
              fi
            done
          else
            echo "✅ No changes to commit"
          fi

      - name: GitHub Cleanup Summary
        run: |
          echo "## GitHub Cleanup Summary 🧹" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ GitHub repository cleanup completed!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Only show file count if data directory exists
          if [ -d "data/" ]; then
            json_count=$(find data/ -name "*.json" -type f 2>/dev/null | wc -l)
            echo "📊 Remaining JSON files in GitHub: **$json_count**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Data Folders:" >> $GITHUB_STEP_SUMMARY
            du -sh data/*/ 2>/dev/null | while read size folder; do
              echo "- $folder: $size" >> $GITHUB_STEP_SUMMARY
            done || echo "No data folders found" >> $GITHUB_STEP_SUMMARY
          else
            echo "📂 No data directory found" >> $GITHUB_STEP_SUMMARY
          fi

  cleanup-ec2:
    needs: start-ec2
    runs-on: self-hosted
    steps:
      - name: Display EC2 disk usage before cleanup
        run: |
          echo "💾 EC2 Disk usage BEFORE cleanup:"
          df -h / || true
          echo ""
          echo "📁 Data folders sizes:"
          du -sh data/*/ 2>/dev/null || echo "No data folders found"
          echo ""
          echo "📊 Total data directory size:"
          du -sh data/ 2>/dev/null || echo "No data directory"

      - name: Clean EC2 data folders
        run: |
          echo "🗑️ Removing all regional data folders on EC2..."

          # Remove all region data folders
          rm -rf data/EastIndia/ || true
          echo "  - Removed EastIndia/"

          rm -rf data/WestIndia/ || true
          echo "  - Removed WestIndia/"

          rm -rf data/NorthIndia/ || true
          echo "  - Removed NorthIndia/"

          rm -rf data/SouthIndia/ || true
          echo "  - Removed SouthIndia/"

          rm -rf data/CentralIndia/ || true
          echo "  - Removed CentralIndia/"

          rm -rf data/NorthEastIndia/ || true
          echo "  - Removed NorthEastIndia/"

          rm -rf data/UnionTerritoriesAndCentral/ || true
          echo "  - Removed UnionTerritoriesAndCentral/"

          echo "✅ All regional data folders removed!"

      - name: Clean GitHub Actions workspace
        run: |
          echo "🧹 Cleaning GitHub Actions workspace..."

          # Clean temp files
          rm -rf /home/*/actions-runner/_work/_temp/* || true
          echo "  - Cleared _temp/"

          # Clean actions cache (but not the entire _actions folder to preserve downloaded actions)
          find /home/*/actions-runner/_work/_actions -type f -name "*.log" -delete 2>/dev/null || true
          echo "  - Cleared action logs"

          echo "✅ Workspace cleaned!"

      - name: Clean pip cache
        run: |
          echo "🧹 Cleaning pip cache..."
          rm -rf ~/.cache/pip/* || true
          pip3 cache purge 2>/dev/null || true
          echo "✅ Pip cache cleaned!"

      - name: Clean Python cache
        run: |
          echo "🧹 Cleaning Python cache..."
          find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
          find . -type f -name "*.pyc" -delete 2>/dev/null || true
          find . -type f -name "*.pyo" -delete 2>/dev/null || true
          echo "✅ Python cache cleaned!"

      - name: Clean temp files
        run: |
          echo "🧹 Cleaning temp files..."
          sudo rm -rf /tmp/* || true
          echo "✅ Temp files cleaned!"

      - name: Clean old logs
        run: |
          echo "🧹 Cleaning old system logs (keeping last 1 day)..."
          sudo journalctl --vacuum-time=1d || true
          echo "✅ System logs cleaned!"

      - name: Display EC2 disk usage after cleanup
        run: |
          echo ""
          echo "💾 EC2 Disk usage AFTER cleanup:"
          df -h / || true
          echo ""
          echo "📊 Data directory size:"
          du -sh data/ 2>/dev/null || echo "No data directory"
          echo ""
          echo "✅ Cleanup completed - disk space freed!"

      - name: EC2 Cleanup Summary
        run: |
          echo "## EC2 Cleanup Summary 🧹" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ EC2 cleanup completed successfully!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Cleaned Items:" >> $GITHUB_STEP_SUMMARY
          echo "- 🗑️ All regional data folders (EastIndia, WestIndia, NorthIndia, SouthIndia, CentralIndia, NorthEastIndia, UnionTerritoriesAndCentral)" >> $GITHUB_STEP_SUMMARY
          echo "- 📦 GitHub Actions workspace cache" >> $GITHUB_STEP_SUMMARY
          echo "- 🐍 Python and pip cache" >> $GITHUB_STEP_SUMMARY
          echo "- 🗄️ Temporary files" >> $GITHUB_STEP_SUMMARY
          echo "- 📋 Old system logs (older than 1 day)" >> $GITHUB_STEP_SUMMARY
          echo "- 🐳 Docker cache (if available)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Disk Usage:" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          df -h / | tail -n 1 >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

  stop-ec2:
    needs: [start-ec2, cleanup-github, cleanup-ec2]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Stop EC2
        run: |
          STATE=$(aws ec2 describe-instances --instance-ids ${{ secrets.EC2_INSTANCE_ID }} \
            --query 'Reservations[0].Instances[0].State.Name' --output text)

          if [ "$STATE" = "running" ]; then
            aws ec2 stop-instances --instance-ids ${{ secrets.EC2_INSTANCE_ID }}
            echo "✅ EC2 stopped successfully!"
          else
            echo "ℹ️  EC2 already in state: $STATE (no action needed)"
          fi
